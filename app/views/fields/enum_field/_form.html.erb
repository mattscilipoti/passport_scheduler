<div class="field-unit__label">
  <%= f.label field.attribute %>
</div>
<%= f.select(
  field.attribute,
  options_for_select(
    f.object.class.public_send(field.attribute.to_s.pluralize) # method to list enum options
      .map { |k, v| [k.humanize, k] },
      f.object.public_send(field.attribute.to_s)),
  include_blank: false) %>
